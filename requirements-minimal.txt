# Minimal dependencies for chat + runtime (no GPU, no desktop automation)
# Install: pip install -r requirements-minimal.txt

numpy>=1.24
psutil>=5.9
pyyaml>=6.0
scipy>=1.10
pillow>=10.0
jieba>=0.42.1
pypinyin>=0.53
ftfy>=6.2
langdetect>=1.0.9
python-dotenv>=1.0
openai>=2.0
requests>=2.28
tqdm>=4.64
